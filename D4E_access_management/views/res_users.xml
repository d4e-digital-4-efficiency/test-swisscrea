<?xml version="1.0"?>
<odoo>
    <data>

        <!-- Update user form !-->
        <record id="view_users_form_forum" model="ir.ui.view">
            <field name="name">res.users.form.forum</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <field name="active" position="after">
                    <field name="permitted_category_id" widget="many2many_tags"/>
                    <field name="product_tag_id" widget="many2many_tags"/>
                    <field name="permitted_website_id" widget="many2many_tags"/>
                </field>
            </field>
        </record>

    </data>
</odoo>
