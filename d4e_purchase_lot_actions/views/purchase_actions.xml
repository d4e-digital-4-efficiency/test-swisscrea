<?xml version="1.0" encoding="utf-8"?>
<odoo>


    <record id="action_purchase_confirm_demands" model="ir.actions.server">
        <field name="name">Confirm Orders</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.button_confirm()
        </field>
    </record>

    <record id="action_purchase_send_demands" model="ir.actions.server">
        <field name="name">Send POs by Email</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_pos_send()
        </field>
    </record>

</odoo>